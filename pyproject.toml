[project]
name = "task_sources"
version = "0.1.0"
description = "Система приёма задач с использованием Protocol и duck typing"
requires-python = ">=3.14"
dependencies = [
    "pytest>=7.0"
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]